version: '3.8'

services:
  unlimited-rush:
    build: .
    container_name: unlimited-rush-game
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      # Persistent local high scores and cloud save profiles
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
